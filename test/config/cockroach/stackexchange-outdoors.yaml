schemaVersion: "2.0.0"

fileExistenceTests:
  - name: "outdoors init script exists"
    path: "/docker-entrypoint-initdb.d/outdoors.sql"
    shouldExist: true

fileContentTests:
  - name: "outdoors declares all expected tables"
    path: "/docker-entrypoint-initdb.d/outdoors.sql"
    expectedContents:
      - "CREATE TABLE Users[ (]"
      - "CREATE TABLE Posts[ (]"
      - "CREATE TABLE Comments[ (]"
      - "CREATE TABLE Votes[ (]"
      - "CREATE TABLE Badges[ (]"
      - "CREATE TABLE Tags[ (]"
      - "CREATE TABLE PostLinks[ (]"
      - "CREATE TABLE PostHistory[ (]"

  - name: "outdoors loads data into representative tables"
    path: "/docker-entrypoint-initdb.d/outdoors.sql"
    expectedContents:
      - "IMPORT INTO Users[ (]"
      - "IMPORT INTO Posts[ (]"
      - "IMPORT INTO PostHistory[ (]"
