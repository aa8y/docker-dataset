#!/usr/bin/env bash
# Ship the OpenFlights data files next to the init script so the authored
# schema's \copy directives resolve at container start (paired with cdDir in the
# manifest).
set -e
mkdir -p /docker-entrypoint-initdb.d/openflights
mv airports.dat airlines.dat routes.dat /docker-entrypoint-initdb.d/openflights/
