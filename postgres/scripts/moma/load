#!/usr/bin/env bash
# Ship MoMA's CSV data next to the init script so the authored schema's \copy
# directives resolve at container start (paired with cdDir in the manifest).
set -e
mkdir -p /docker-entrypoint-initdb.d/moma
mv Artworks.csv Artists.csv /docker-entrypoint-initdb.d/moma/
