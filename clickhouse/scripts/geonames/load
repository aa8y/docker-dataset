#!/usr/bin/env bash
# Ship the GeoNames export next to the init script so the `INSERT ... FROM
# INFILE` the transform hook appended resolves at container start.
set -e
mkdir -p "/docker-entrypoint-initdb.d/${DATASET}"
mv cities15000.txt "/docker-entrypoint-initdb.d/${DATASET}/"
