#!/usr/bin/env bash
# Ship the OpenFlights data files next to the init script so the `INSERT ...
# FROM INFILE` statements the transform hook appended resolve at container start.
set -e
mkdir -p "/docker-entrypoint-initdb.d/${DATASET}"
mv airports.dat airlines.dat routes.dat "/docker-entrypoint-initdb.d/${DATASET}/"
